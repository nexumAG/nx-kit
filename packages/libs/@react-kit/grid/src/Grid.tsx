import React from 'react';
import { GridProps } from './Grid.types';

export const Grid = ({ className }: GridProps) => {
  return <div className={className}>the snozzberries taste like snozzberries</div>;
};
