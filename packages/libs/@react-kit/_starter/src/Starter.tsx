import React from 'react';
import { StarterProps } from './Starter.types';

export const Starter = ({ className }: StarterProps) => {
  return (
    <div className={className}>the snozzberries taste like snozzberries</div>
  );
};
